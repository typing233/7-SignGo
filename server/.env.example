# Gmail SMTP 配置
# 使用 Gmail 应用专用密码（非登录密码）
# 获取方法: Google 账号 → 安全性 → 两步验证 → 应用专用密码
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-password

# 应用地址（签署链接的前缀）
APP_URL=http://localhost:5173

# JWT 密钥
JWT_SECRET=signgo-dev-secret-change-in-prod

# 服务器端口
PORT=3002
